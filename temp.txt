app.get('/api/users', (req, res) => {
    userModel.find({}, (err, result) => {
        if (err) {
            console.log(`Error. ${err}`);
        } else {
            res.json();
            console.log('it works');
        }
    });
});